{
	"scripts": {
		"server": "cd blog-back && nodemon server",
		"client": "cd blog-front && npm start",
		"start": "concurrently --kill-others-on-fail \"npm run blog-back\" \"npm run blog-client\""
	},
	"dependencies": {
		"concurrently": "^7.4.0",
		"express": "^4.18.1",
		"nodemon": "^2.0.20"
	}
}
